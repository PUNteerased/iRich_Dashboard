# Vercel env (Settings → Environment Variables), then Redeploy:
#   TELEMETRY_UPSTREAM=https://xxxx.trycloudflare.com
#   (or your ngrok URL — free ngrok may hit ERR_NGROK_727 monthly limit)
# Browser on Vercel uses same-origin /api/bridge (no CORS).
NEXT_PUBLIC_TELEMETRY_URL=http://127.0.0.1:8000
